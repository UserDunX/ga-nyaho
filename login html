<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Website Kelas</title>
    <link rel="stylesheet" href="style.css">
    <style>
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background: url('bg.jpg') no-repeat center center fixed; /* Gambar latar belakang */
            background-size: cover;
            height: 100vh;
            overflow: hidden; /* Menyembunyikan scroll jika konten melebihi tinggi layar */
        }

        .login-container {
            width: 400px;
            margin: 100px auto;
            background: rgba(255, 255, 255, 0.9); /* Latar belakang semi-transparan */
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
            text-align: center;
            position: relative;
            z-index: 1;
        }

        .login-container h2 {
            color: #0056b3;
            margin-bottom: 30px;
            font-size: 2.5em;
        }

        .form-group {
            margin-bottom: 25px;
            text-align: left;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #555;
            font-weight: bold;
        }

        .form-group input {
            width: 100%;
            padding: 14px;
            border: 1px solid #ddd;
            border-radius: 6px;
            box-sizing: border-box;
            font-size: 16px;
            transition: border-color 0.3s;
        }

        .form-group input:focus {
            border-color: #0056b3;
            outline: none;
        }

        .form-group button {
            background-color: #0056b3;
            color: #fff;
            padding: 14px 25px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 18px;
            transition: background-color 0.3s ease;
            width: 100%;
        }

        .form-group button:hover {
            background-color: #003d80;
        }

        /* Efek Paralaks */
        .parallax {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            pointer-events: none;
        }

        .parallax img {
            position: absolute;
            width: 200px;
            opacity: 0.7;
            animation: float 4s infinite alternate;
        }

        .parallax img:nth-child(1) {
            top: 20%;
            left: 10%;
            animation-delay: 0s;
        }

        .parallax img:nth-child(2) {
            top: 40%;
            left: 30%;
            animation-delay: 1s;
        }

        .parallax img:nth-child(3) {
            top: 60%;
            left: 50%;
            animation-delay: 2s;
        }

        .parallax img:nth-child(4) {
            top: 80%;
            left: 70%;
            animation-delay: 3s;
        }

        @keyframes float {
            0% {
                transform: translateY(0);
            }
            100% {
                transform: translateY(-20px);
            }
        }
    </style>
</head>
<body>
    <div class="parallax">
        <img src="icon1.png" alt="Icon">
        <img src="icon2.png" alt="Icon">
        <img src="icon3.png" alt="Icon">
        <img src="icon4.png" alt="Icon">
    </div>

    <div class="login-container">
        <h2>Selamat Datang!</h2>
        <form action="index.html" method="get">
            <div class="form-group">
                <label for="username">Username:</label>
                <input type="text" id="username" name="username" required>
            </div>
            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <div class="form-group">
                <button type="submit">Login</button>
            </div>
        </form>
    </div>
</body>
</html>
